```bash

nodemon --exec vite build & electron . --watch ./src --ext .js, .html, .css, .vue

npm install electron-win-state -D


// 安装sass

npm i --save-dev sass

# 安装element-plus
npm install  element-plus --save

# 安装@electron/remote
npm install -save @electron/remote

安装Vuex
npm install vuex@next --save

安装vue-router
npm install vue-router@4

安装 mockjs
npm install mockjs -D

安装 axios
npm install axios

安装 vue-axios
npm install vue-axios

安装qs
将{}(对象转成name=zhanshan&age=18的形式)
npm install qs
```

